input{
    kafka{
        bootstrap_servers => ["localhost:9092"]
        client_id => "test"
        group_id => "test"
        auto_offset_reset => "latest"
        consumer_threads => 5
        decorate_event => true
        topics => ["test2topicganlin"]
        type => "bhy"
    }
}
output{
    elasticsearch{
        hosts => ["127.0.0.1:9200"]
        action => "index"
        document_type => "%{type}"
    }
}